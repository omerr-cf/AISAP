services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:3000/api/studies"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 40s
